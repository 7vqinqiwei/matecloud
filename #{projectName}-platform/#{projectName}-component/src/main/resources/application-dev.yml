server:
  port: 20003
spring:
  application:
    name: @artifactId@
  #数据源配置
  datasource:
    url: ${#{projectName}.datasource.url}
    username: ${#{projectName}.datasource.username}
    password: ${#{projectName}.datasource.password}
