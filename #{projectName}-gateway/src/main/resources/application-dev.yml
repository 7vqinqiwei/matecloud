server:
  port: 10001
spring:
  application:
    name: @artifactId@
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
    loadbalancer:
      retry:
        enabled: true
#{projectName}:
  #自定义路径配置
  route:
    config:
      data-id: #{projectName}-gateway-router
      group: DEFAULT_GROUP
  #MineRequestProperties属性配置
  request:
    #traceId跟踪
    trace: true
    #获取IP
    ip: true
    #黑名单增强模式启动
    enhance: true
